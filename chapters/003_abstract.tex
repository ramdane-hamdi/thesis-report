%% =============================================================================
%% ABSTRACT (English + French)
%% The detailed abstract text is commented out; a short placeholder is shown.
%% =============================================================================

% \chapter*{Abstract}
% \addcontentsline{toc}{chapter}{Abstract}


% Original English abstract (kept commented for future use)
% \clearpage
% 
% \begin{center}
% \small\textbf{Abstract}
% \end{center}
% \begin{flushleft}
% This work looks at a two-phase reinforcement learning system for endoscope
% navigation and snare capture in a Unity simulation. The important point is that the
% project does not try to detect polyps directly. Instead, it trains vision-only agents
% to move through a colon-like environment and then perform a capture task on a
% predefined polyp object.
% 
% The work is split into two parts because the full problem is easier to study that way.
% In Phase 1, the agent learns how to steer the endoscope tip while forward motion is
% scripted. In Phase 2, the agent learns how to extend, rotate, and close a snare so it
% can reach the target object safely. Both phases use PPO, a convolutional visual
% encoder, and memory to keep track of short-term context.
% 
% The document also explains the design choices behind the simulation, the reward
% functions, the evaluation setup, and the limits of this approach. The goal is to show
% how reinforcement learning can be used for procedural control in a clean simulated
% environment before any real-world step is considered.
% 
% \noindent\\textbf{Keywords:} endoscopy, reinforcement learning, PPO, Unity, simulation, snare capture
% \end{flushleft}
% 
% \clearpage
% 
% \begin{center}
% \small\textbf{R\'esum\'e}
% \end{center}
% \begin{flushleft}
% Ce m\'emoire pr\'esente un syst\`eme en deux phases bas\'e sur l'apprentissage par
% renforcement pour la navigation endoscopique et la capture avec une anse dans une
% simulation Unity. Le point important est que le projet ne fait pas de d\'etection de
% polypes. Il apprend plut\^ot \`a des agents visuels \`a se d\'eplacer dans un environnement
% de type colon et \`a r\'ealiser ensuite une t\^ache de capture sur un objet polype
% pr\'e-d\'efini.
% 
% Le travail est divis\'e en deux phases pour garder le probl\`eme plus simple. Dans la
% Phase 1, l'agent apprend \`a orienter l'extr\'emit\'e de l'endoscope pendant que la pouss\'ee
% avant est script\'ee. Dans la Phase 2, l'agent apprend \`a d\'eployer, faire tourner et
% fermer une anse pour atteindre la cible sans actions inutiles. Les deux phases utilisent
% PPO, un encodeur visuel convolutionnel et une m\'emoire courte pour mieux suivre le
% contexte.
% 
% Le m\'emoire explique aussi les choix de simulation, les fonctions de r\'ecompense, le
% protocole d'\'evaluation et les limites de la m\'ethode. L'objectif est de montrer comment
% l'apprentissage par renforcement peut aider pour le contr\^ole proc\'edural dans un
% environnement simul\'e avant toute utilisation r\'eelle.
% 
% \noindent\textbf{Mots-cl\'es:} endoscopie, apprentissage par renforcement, PPO, Unity, simulation, capture par anse
% \end{flushleft}
% 
% \clearpage

